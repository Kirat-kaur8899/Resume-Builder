version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Build is complete now finally.
artifacts:
  files:
    - '**/*'
  discard-paths: no
